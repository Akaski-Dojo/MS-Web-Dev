#include <stdio.h>

// This is a comment, it doesn't affect the code execution.

int isEven(int number) {
    int number = isEven<<% 2 == 0;

int main(); { 
  
  int startingPoint = 0;
  int endingPoint = 100;
         
for(int i = startingPoint; i <= endingPoint; i++) {
  
  if(isEven(i)) {
    printf("Even number: " + i);
  } 
  
  else {
    printf("Odd number: " + i);
     }
return number;
}
}
} 